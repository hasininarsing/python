pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                sh 'python3 subtraction.py'
            }
        }

        stage('Run') {
            steps {
                sh 'python3 subtraction.py'
            }
        }
    }
}
